{
  "task_id": "actionlint-action-pinning-lint",
  "purpose": "Exercise the regression stage on a real DeepSWE task. The repairs are hand-written harness inputs, not attempts to solve the task.",
  "suite": {
    "test_id": "go-root-package",
    "targets": ["."],
    "timeout_seconds": 1800
  },
  "repairs": {
    "repair_00_malformed.txt": "no edit block; rejected before validation",
    "repair_01_drop_glob_validation.txt": "removes ParseConfig's glob check; expected to break TestConfigParseError/invalid_glob_pattern",
    "repair_02_config_section.txt": "adds an unused config field; expected to keep every baseline-passing test passing"
  },
  "regression_exclusions": "TestConfigGenerateDefaultConfigFileOK: adding a config section may legitimately change the generated default config file"
}
